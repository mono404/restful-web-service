server:
  port: 8088

logging:
  level:
    org.springframework: DEBUG

spring:
  messages:
    basename: messages
  mvc:
    view:
      prefix: "/WEB-INF/views/"
      suffix: ".jsp"

management:
  endpoints:
    web:
      exposure:
        include: "*"

