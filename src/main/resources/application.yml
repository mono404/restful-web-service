server:
  port: 8088

logging:
  level:
    org.springframework: DEBUG
  file:

spring:
  messages:
    basename: messages
  mvc:
    view:
      prefix: "/WEB-INF/views/"
      suffix: ".jsp"
  servlet:
    multipart:
      enabled: true
      max-file-size: 200MB
      max-request-size: 215MB

management:
  endpoints:
    web:
      exposure:
        include: "*"

file:
  upload-dir:"Users\user\IdeaProjects\restful-web-service"